# Public backend origin baked into production builds (Vercel + any static host).
# Not a secret — this is the public API URL. Same-origin deploys also work with it.
VITE_API_URL=https://mara-backend-28va.onrender.com
