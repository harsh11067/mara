# Backend origin (REST + WebSocket). Local default:
NEXT_PUBLIC_API_URL="http://localhost:3001"
# Google OAuth client ID (public identifier) — enables the Google sign-in button.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""
